# DEPLOY.md — Production deploy rules for DataWise

**READ THIS BEFORE ANY DEPLOY. Especially Claude/Codex/AI agents.**

## The one rule that matters

> The `production` branch is the live site. Every change goes through a PR into `production`, gets merged, and is deployed using **only** `npm run deploy:pages:production` from a clean checkout of `production`. Never run raw `wrangler pages deploy` against this project.

Why: `npm run deploy:pages:production` runs the guard in `datawise-seo-insight-main/scripts/deploy-pages-production.mjs`, which refuses to deploy if the built bundle is missing critical features (Content Writer, AI Visibility, Brand Tracker, People Also Ask, Fan-out Queries, Content Planner, Content Tools, the indexation chart, the keyword metric badges, etc.) or if the current branch is not `production`. Raw `wrangler pages deploy` bypasses every one of those checks and will ship broken builds.

## Branch model

- **`production`** = trunk. What's live at `datawiseseo.com`.
- **`main`** = legacy, stale. Do not use.
- **Feature branches**: any name, branched off `production`, merged back into `production` via PR.

## Workflow for every change (including small fixes)

Promotion order is mandatory:

1. Build/test locally.
2. Deploy and verify a Cloudflare Pages preview/staging URL.
3. Deploy production last.

Never use `datawiseseo.com` as the first test target. If there is no preview/staging URL for the change, stop and create one before production deploy.

```sh
# 1. Sync with live
cd "/Users/nicolasgorrono/Desktop/DataWise V2"
git fetch origin
git checkout production
git pull origin production

# 2. Branch
git checkout -b fix/short-description    # or feat/short-description

# 3. Edit + test locally
cd datawise-seo-insight-main
npm run dev   # frontend on :8080

# 4. Commit + push
git add <specific files, no `git add .`>
git commit -m "fix: …"
git push -u origin fix/short-description

# 5. Open PR on GitHub: base = production, compare = fix/...
#    Review the diff carefully. If files you didn't change are in the diff, STOP.

# 6. Deploy/verify preview or staging.
#    Use the PR/feature preview URL or a staging Pages deployment.
#    Verify the actual user flows touched by the change before merge.

# 7. Merge PR (squash or merge — either is fine)

# 8. Deploy production last
git checkout production
git pull origin production
cd datawise-seo-insight-main
npm run deploy:pages:production
# This script:
#   - asserts current branch == production
#   - builds with the production VITE_API_URL
#   - greps the built bundle for required feature markers
#   - aborts if anything critical is missing
#   - only then uploads via wrangler

# 9. Tag the deploy
cd "/Users/nicolasgorrono/Desktop/DataWise V2"
TS=$(date -u +%Y-%m-%d-%H%M)
git tag -a "prod-$TS" -m "deploy"
git push origin --tags
```

## Worker (API) deploys

The Worker is a separate codebase in `datawise-seo-insight-main/workers/`. Worker deploys are independent of the SPA:

```sh
cd datawise-seo-insight-main/workers
npm run deploy     # → wrangler deploy → datawise-api (no env flag)
```

DO NOT use `npm run deploy:production` for the worker — see `~/.claude/projects/-Users-nicolasgorrono-Desktop-DataWise-V2/memory/reference_deployment.md` for the naming trap (creates an orphan `datawise-api-production` worker).

## Rollback (Pages)

If a deploy goes wrong, rollback via Cloudflare API (wrangler CLI does not support Pages rollback):

```sh
# 1. List recent deployments to pick the one to restore
cd datawise-seo-insight-main
CLOUDFLARE_ACCOUNT_ID=510d0ac03a3a8f5ebeac39be4926ed77 \
  npx wrangler pages deployment list --project-name=datawise | head -10

# 2. Rollback to that deployment id
DEPLOYMENT_ID=<paste-id-here>
CF_TOKEN=$(grep -E "^oauth_token|^api_token" ~/Library/Preferences/.wrangler/config/default.toml | head -1 | cut -d'"' -f2)
curl -X POST \
  "https://api.cloudflare.com/client/v4/accounts/510d0ac03a3a8f5ebeac39be4926ed77/pages/projects/datawise/deployments/$DEPLOYMENT_ID/rollback" \
  -H "Authorization: Bearer $CF_TOKEN" \
  -H "Content-Type: application/json"
```

Then verify by curling `https://datawiseseo.com/` and checking the `assets/index-XXXX.js` filename matches the deployment you rolled back to.

Named recovery tags (use `git checkout <tag>` to restore source state):

- `prod-2026-05-13-2327-live` — last known-good live state before the 2026-05-14 incident.
- `prod-2026-05-21-1435` — after worker+SPA: typed `DataForSeoQuotaError` + 503 friendly message for `provider_quota_exhausted` (Bob's rank-tracking 402).
- `prod-2026-05-21-1504` — after worker-only perf pass: caching on keywords/competitors/rank-tracking/local-seo DFS calls, cron `*/2 → */5`, KV short-circuit on DFS 402. Worker version `012530e8`.
- `prod-2026-05-21-1552` — Brand Tracker per-platform split (fixes imregabri's `e0c73d59` concurrency bug); AI Visibility tabs trimmed to AI Search Tracker + Brand Tracker (removed duplicate People Also Ask + orphan On-Page SEO). Merge commit `96205a4`, PR #16. SPA-only; rollback via `git revert 96205a4 && git push origin production`.
- `prod-2026-05-27-1228` — OpenRouter preflight follow-ups: worker returns 400 (not 401) when key is invalid so SPA's auth interceptor stops force-logging users out (bug `7b7e46d1`); SPA `getLLMConfig()` tolerates legacy stored provider when api_key starts with `sk-or-`, SettingsPage auto-purges stale legacy config (bug `663ce49c`). Merge commit `5950c57`, PR #25. Worker version `fa2c7495`. Rollback both: `git revert 5950c57 && git push origin production` AND `wrangler rollback --message "openrouter preflight regression" 012530e8`.
- `prod-2026-05-27-1307` — GSC cluster 1: atomic `env.DB.batch` disconnect (bug `f83f0ecd`), refuse OAuth callback with empty `refresh_token` (the source of orphan-connection state), `refresh_failed_at` tracking, SPA reconnect/cleanup banner. New D1 column `gsc_connections.refresh_failed_at` (additive, already applied). Merge commit `8271103`, PR #26. Worker version `64ae2d0e`. Rollback both: `git revert 8271103 && git push origin production` AND `wrangler rollback --message "gsc cluster 1 regression" fa2c7495`. The D1 column is safe to leave in place even after rollback (old code ignores it).
- `prod-2026-05-27-1324` — SPA-only UX cluster: usePersistentState hook persists keyword research + Brand Tracker query inputs/results across navigation (bug `252b2580`); Content Writer empty-interview "Start interview" button (bug `44aba545`); Top Pages tile URL clipping with flex+min-w-0+truncate (bugs `bac2d060`, `dc9af2b2`). Merge commit `7ca0bcc`, PR #27. SPA-only; rollback via `git revert 7ca0bcc && git push origin production`. localStorage keys under `datawise:page-state:v1:*` are versioned for forward-compat.
- `prod-2026-05-27-1352` — Meta Checker apostrophe truncation: the `content="…"` regex used character classes on both sides, so any value containing `'` was clipped at the first apostrophe (Kendrick / dcroofingarizona.com: 158-char description read as 16-char "Extend your roof"). Switched to backreference `(["'])(...)\1` in `meta-checker.ts` `extractMetaDescription` + `extractMetaKeywords` + their `name|property` sub-extractors. Worker-only. Merge commit `a01828c`, PR #28. Worker version `fd479d13`. Rollback both: `git revert a01828c && git push origin production` AND `wrangler rollback --message "meta-checker apostrophe fix regression" 64ae2d0e`.
- `prod-2026-05-27-1737` — Bug batch 4 (SPA-only). Competitor Analysis tabs (Domain Rank, Ranked Keywords, Gap Analysis, Traffic, Competitors) now persist inputs + results via usePersistentState so tab switches no longer wipe state (bug `6e758797`). SEO Assistant reads its GSC property from PropertyContext and the in-page selector is now a read-only indicator, eliminating the two-selector confusion (bug `89b32130`). Brand Tracker free-text filter widened to match across question + answer + source titles/domains/snippets, fixing multi-word queries returning empty (bug `2119ccb9`). Merge commit `1e0ef5d`, PR #29. SPA-only; rollback via `git revert 1e0ef5d && git push origin production`.
- `prod-2026-05-27-1849` — Content Writer connection-drop recovery (SPA-only). When Research/Outline/Draft worker calls run long (60-120s) an intermediary (CF edge, proxy, dropped wifi) can close the connection while the worker keeps running and persists the output. The browser surfaced this as a generic "Failed to fetch" TypeError and the user was stranded. `runStep` now catches network drops, waits 8s, refetches the post, and if the step output is saved silently treats the click as success; otherwise it shows a friendlier "connection dropped, click again" message instead of "Failed to fetch". Fixes bug `0ac02a93` (Jon Petruch). Merge commit `a16b81e`, PR #31. SPA-only; rollback via `git revert a16b81e && git push origin production`.
- `prod-2026-06-03-1246` — Crawler UA + DFS false-negative fixes (worker-only). Customer sites behind WooCommerce/Cloudflare WAFs return 403 to our self-identifying `DataWiseBot/1.0` UA but 200 to a browser UA. Caused Meta Checker nil title/description and failed sitemap discovery (bug `7953e920`, bradsbikes.com.au) and Site Audit false "No robots.txt found" / "no sitemap" (bug `a9befa95`, resortstylebeanbags.com.au). Centralized a verified browser UA as `BROWSER_UA` in `safe-fetch.ts`, used it in `meta-checker.ts` + `content-tools.ts` sitemap discovery; added `clearRobotsSitemapFalseNegatives()` in the site-audit finalizer to re-verify robots.txt/sitemap with a browser UA before trusting a DFS "missing" verdict; reworded the schema-validation finding to point to Google Rich Results Test instead of validator.schema.org. Merge commit `6ee6596`, PR #40. Worker version `df7021dd`. Rollback: `wrangler rollback --message "crawler UA regression" 5193ad63` (worker is the only surface; no SPA change to revert).
- `prod-2026-06-03-1459` — People Also Ask hang fix (worker + SPA). PAA ran a sequential BFS of up to 10 (depth 2) / 25 (depth 3) DataForSEO `live/advanced` SERP calls awaited one at a time, taking 5+ minutes with only a spinner (bug `d45587d5`, info@nahiro.net). Fix: dispatch each BFS round as up to 6 single-task calls in parallel via `Promise.all` (NOT batched into one POST — DFS live/advanced only processes the first task in an array, the rest return status 40000 empty; verified against live DFS), and add `people_also_ask_click_depth: 4` so DFS expands the PAA accordion server-side (~12 questions/call vs ~4). Verified: "lapis lazuli" depth 3 → 50 unique questions in ~18s (was ~10 / multi-minute). SPA adds an elapsed-seconds progress counter. Merge commit `ee6ca5a`, PR #42. Worker version `9bed6b83`. Rollback both: `git revert ee6ca5a && git push origin production` AND `wrangler rollback --message "paa regression" df7021dd`.
- `prod-2026-06-03-2034` — Content Revival rewrite token-cap fix (worker-only). `handleRewritePost` capped LLM output at 4096 tokens while `REWRITE_PROMPT` asks for a full article (min 1,200 words) + FAQ. On reasoning models (deepseek-v4, gpt-5*) reasoning tokens are deducted from that budget, so the model could spend the cap and return only the opening `# title`, leaving a title with no body (bug, javier.barrezueta; worse in Spanish since output runs ~15-20% longer). Same failure mode already fixed for the Writer (`content-writer.ts` resolvePostStepMaxTokens, draft=16384) but never propagated to the rewriter. Fix: raise rewrite + language-retry cap to 16384; guard against silent empty/title-only output (return a clear 502 instead of a blank rewrite) and log when output hits the cap. Merge commit `c697331`, PR #44. Worker version `b11eb18a`. Rollback: `wrangler rollback --message "revival token cap regression" 9bed6b83` (worker only; no SPA change to revert).
- `prod-2026-06-10-1132` — GSC incremental sync (worker-only). `handleGSCSync` no longer wipes and reloads the full window per sync: converted properties (have `source='pd'` rows) refetch per-day rows only from 2 days before the newest stored pd date, delete only the rewritten row sets, trim pd at 35 days, and scheduled (cron) syncs reuse the agg90 aggregate for up to 7 days (`trigger:'scheduled'`; the manual Sync button stays fully fresh). First syncs / legacy properties keep the full wipe-and-reload path, which converts them. Kills the dominant D1 rows-written cost (~90% per-cron-sync cut measured on alignlending.com: 5,801 rows full vs ~600 incremental). Merge commit `6250c47`, PR #48. Worker version `46f5f88f`. NOTE: `46f5f88f` was built from `feat/ai-visibility-tracker` (PR #47, already live as `f31e4993`) plus this fix, because the live worker was ahead of `production` with the unmerged tracker. Rollback: `wrangler rollback --message "gsc incremental sync regression" f31e4993` (worker only; no SPA change to revert; `f31e4993` still contains the tracker).

- `prod-2026-06-10-1626` — Triple merge (worker + SPA): PR #47 AI Visibility Tracker (weekly Monday cron, kill switch `ai-tracking-paused`), PR #49 Rank Tracking wave 1 (scheduled SERP checks Tue/Thu/Sat 08:00 UTC with kill switch `rank-checks-paused` + 200 kw/run cap, single-task DFS fix so all keywords actually get checked, honest >100 misses, device column, window-function queries, Local Pack 5x parallel checks, single property selector, enriched project cards, debounce, pagination), PR #50 AI Visibility panel redesign (verdict strip, rule-based recommendations, cited-terms discovery tab via llm-mentions, answer_text storage + lazy endpoint, 20-query cap, Competitor Comparison card removed, vitest added to workers/ with 12 tests). Merge commits `032ac0a`-stack -> `d642f93` (#49) -> `68e5c93` (#50). Worker version `503007d5` (already live pre-merge; merges reconciled git with the deployed worker). D1 migrations applied: ai-visibility-tracking (2026-06-09), tracked_keywords.device, ai_visibility_checks.answer_text (all additive). Rollback: SPA via Pages rollback to the pre-merge deployment; worker `wrangler rollback --message "triple-merge regression" f31e4993` (pre-wave worker, still contains the tracker).

- `prod-2026-06-10-2024` — Local Pack experience (worker + SPA), PR #51, merge commit `0cfc6e7`. Reviews report (header tiles with period deltas, rating distribution, LLM review themes cached in D1 `local_review_themes` keyed by review-set hash, themes/trend toggle with monthly sentiment chart, filterable review list with clickable unanswered-low-star tile and theme filters, depth 100 only when project_id present so the old SPA stays at 20); `local_review_snapshots` daily snapshots power all trends; geo-grid radius-derived zoom replaces hardcoded 17z (`zoomForRadius`), per-scan competitor share persisted to `geogrid_competitors` (incl `is_user`) with Who-owns-your-map UI + series endpoint; period-performance report endpoint (D1/KV only) + PDF/DOCX export adapter; review-task polling extended to ~40s for deep fetches; renderPdf date separator made ASCII (also fixes organic export). D1 migration `2026-06-10-local-pack-experience.sql` applied to prod (3 tables) BEFORE worker deploy (read paths query them). Worker version `43a554a0` (chain: 2a1dfb17 -> b5b26257 -> 503007d5). 25 new vitest tests (37 total). Rollback: SPA via Pages rollback to the pre-merge deployment; worker `wrangler rollback --message "local pack regression" 503007d5`. New tables are additive and safe to leave in place.

- `prod-2026-06-11-1426` — Brand Tracker compare fix (worker + SPA), PR #52, merge commit `1dd94ce`. Bug `5308c018` (imregabri, critical): compare showed "No comparison data returned" because (1) DFS task-level errors (HTTP 200 + tasks[0].status_code 40xxx) were coerced to null by `extractResult` and rendered as the empty state, (2) `dataforseoRequestCached` cached failed task responses for the full 6h TTL, pinning transient failures, (3) DFS domain matching is case-sensitive and Brand Tracker never lowercased the analyzed domain ("GrowthToday.co" = 0 mentions vs "growthtoday.co" = 14). Fix: `getTaskError`/`isCacheableDfsResponse` in `dataforseo/client.ts` (failures never cached, applies to all cached DFS callers), all 6 llm-mentions handlers return 502 + DFS status message, BrandTracker lowercases on input and on read of persisted value. 6 new vitest tests (43 total). Worker version `4ba23009`. Rollback both: `git revert 1dd94ce && git push origin production` AND `wrangler rollback --message "brand tracker compare regression" 43a554a0`.

- `prod-2026-06-11-1451` — GSC cluster 2 (worker + SPA), PR #53, merge commit `995bdc6`. Four bugs: disconnect deleted all gsc_search_data in one statement and failed past D1's 30s cap for large accounts (`ae38480a`, 882k rows) -> chunked 10k-row delete loop before the atomic connection+property batch; Domain vs URL prefix GSC property variants rendered identically (`e7f15a26`) -> type badges + explainer in Settings; PropertyContext picked manual duplicates over GSC twins so the dashboard told connected users to connect (`1027d415`) -> GSC-preferred selection + switch-property/run-sync panel; GSC-imported sites missing from the Websites card (`18125820`) -> GSC rows render there deduped by domain. Worker version `7c555485`. Rollback both: `git revert 995bdc6 && git push origin production` AND `wrangler rollback --message "gsc cluster 2 regression" 4ba23009`.

- `prod-2026-06-11-1519` — GSC cron starvation fix (worker-only), PR #54, merge commit `ef73aa2`. The 2026-05-19 per-day data fix (PR #15) converts properties on re-sync, but `runDailyGSCSync` selected its due set (~3,400 props) with no ORDER BY while one 15-min cron window syncs only a few hundred, so the same tail starved nightly: 273 active users' properties were still on pre-fix single-batchDate data on 2026-06-11. Fix: ORDER BY oldest-synced first / never-synced last (self-rotating), `kind='gsc'` filter (manual/bwt rows occupied 342 slots), 10-min dispatch budget with processed/due rollover log. Per-sync write volume untouched. Worker version `ee96f172`. Rollback: `wrangler rollback --message "gsc cron rotation regression" 7c555485` (worker only; no SPA change to revert). Watch the 11:00 UTC cron summary line for conversion progress.

- `prod-2026-06-11-2353`: Global error boundary + route lazy loading (SPA-only), PR #56, merge commit `9086613`. All 18 authed pages load via React.lazy (entry chunk ~2MB to 540kB); new `AppErrorBoundary` wraps the app (full-screen fallback) and each route inside Layout (sidebar survives page crashes, resets on navigation); stale-chunk failures after a deploy reload once via sessionStorage flag `dw_chunk_reload_at` (60s window) then show a refresh prompt instead of looping. Rollback: `git revert 9086613 && git push origin production`.

- `prod-2026-06-11-2354`: Export everywhere (SPA-only), PR #58, merge commit `7fbebe2`. PDF/DOCX ExportMenu plus CSV/clipboard on the surfaces users requested most (10+ open feedback items): Site Audit, Meta Checker (+CSV), Brand Tracker (+CSV), AI Visibility per engine, Keyword Overview, Keyword Gap, Ranked Keywords, Content Writer brief (+ copy outline/sources as Markdown), Content Planner. 9 new files in `src/lib/export/adapters/` (shared.ts caps tables at 500 rows with omission callout); export engine untouched. Known limit: Site Audit images section exports stored ALT/PNG samples only; full image list needs a worker persistence change. Rollback: `git revert 7fbebe2 && git push origin production`.

- `prod-2026-06-11-2355`: White-label export branding (worker + SPA), PR #57, merge commit `82f7f7e`. Per-account logo/color/name on PDF and DOCX exports via new Export Branding card in Settings; footer attribution "Generated by DataWise SEO" stays on every export by design. Worker routes GET/PATCH `/api/branding`, POST/DELETE `/api/branding/logo` (R2 `TASK_ATTACHMENTS` at `{userId}/branding/{uuid}`, 1MB, png/jpeg/webp). D1: 3 additive nullable `users` columns (`branding_logo_key`, `branding_color`, `branding_name`), applied before worker deploy. Worker version `bef64bac`. NOTE: `bef64bac` was built from `feat/export-branding` merged with unmerged PR #55 (`fix/community-csv-revocation`) because the live worker `1cf322ea` was ahead of `production` with PR #55's admin.ts protections; an interim deploy `0b788bb5` (branding only) briefly reverted them and was corrected within the hour. Always check whether the live worker contains unmerged PR code before deploying. Rollback both: `git revert 82f7f7e && git push origin production` AND `wrangler rollback --message "branding regression" 1cf322ea`. The D1 columns are additive and safe to leave in place.

- `prod-2026-06-25-2322` — GSC sync empty-response guard (worker-only), PR #68. `handleGSCSync` now bails before any mutation when the GSC daily-totals fetch returns 200-but-empty rows, so a manual re-sync right after a site re-verify / URL change no longer DELETEs the `'__daily_total__'` rows the dashboard sums and re-inserts nothing (bug `fc3660cd`: "dashboard showing 0 for all three websites"; data self-healed only after the nightly cron rebuilt the rows). Normal non-empty syncs are byte-for-byte unchanged. Adds the first worker vitest for `sync.ts` + a scoped `vitest.config.ts` (so `npm test` no longer loads the SPA vite config). Worker version `328cbf6b` (built from `origin/production` `9a64137` + the fix; previously-live `42abf228` = production through PR #66, verified nothing reverted). Pre-existing follow-up NOT in this PR: make the daily-total delete+reinsert atomic to also cover a worker dying mid-write (full-table atomicity is impossible via one D1 batch — 1000-stmt cap vs tens of thousands of insert rows). Rollback: `wrangler rollback --message "gsc empty-response guard regression" 42abf228` (worker only; no SPA change to revert).

- `prod-2026-07-01-1420` — Local Pack rating crash + review themes/trend (worker + SPA), PR #72, merge commit `d715fb7`. (1) Critical React error #31 crashed the whole Local Pack tab on first load after project setup: `handleGBPProfile` (and `handleGeoGridInsights`) returned DataForSEO's raw rating object `{rating_type,value,votes_count,rating_max}` whenever a business had no numeric rating (`value` null → the `?? business.rating` fallback leaked the object), and `GBPProfileCard` rendered it as `<span>{rating}</span>` (bugs `a1c1a8841ea6b74425111880376368a9` critical, `0ccc735d20d807c5759a19d553744bf4`). Fixed by extracting `.value` robustly (never returns/stringifies the object; the internal object is kept because `reviews_count` reads `.votes_count`) + a frontend `typeof profile.rating === 'number'` render guard. Worker `cc37a4a5`. (2) "What customers are saying" themes + trend blank for OpenRouter/DeepSeek BYOK users: `validateReviewThemes` was all-or-nothing and rejected the entire LLM response over one sentiment outside `positive|negative|mixed` (DeepSeek V4 emits "neutral" for 3-star reviews) → "The model returned an unreadable response"; and `ThemesPanel` gated the client-side sentiment Trend behind that same themes-error state. Fixed by tolerant validation (`normalizeSentiment` maps neutral/unknown→mixed, drop malformed themes instead of the whole set, tolerate a missing summary), new `extractJsonObject` (fence/prose/trailing-comma tolerant parsing), themes `max_tokens` 4096→8192, and decoupling the Trend view so it renders from review ratings+dates independent of themes loading/error. 81/81 worker vitest pass (TDD: the fix flipped a test that had encoded the buggy all-or-nothing reject). Worker `9a952187`. Both worker versions already live (deployed before the SPA merge, shared API). Rollback both: `git revert d715fb7 && git push origin production` AND `wrangler rollback --message "local pack rating/themes regression" 4807020f` (pre-change worker = PR #70).

- `prod-2026-07-01-1518` — BYOK OpenRouter key silently disappearing on iPad (SPA-only), PR #73, merge commit `e54a45e`. Bug `702e4f2640085782a2903fb102fefa83` (critical, info@clearvisiondigital.io, iPad/Chrome-iOS on `/site-audit`): "Open router API keeps disappearing … even after entering it twice." Root cause: the BYOK key is stored localStorage-only (`datawise_llm_config`, "never sent to our servers") and `saveLLMConfig()` wrote it with no error handling and no read-back. iOS Private Browsing throws on `localStorage.setItem`, and in-app browsers (link opened from another app) / ITP accept the write but drop it, so the save silently failed and every reader (`getLLMConfig()` → SEO Assistant, MetaChecker, Content Writer, geo-grid, reviews) saw null and re-prompted. Ruled out all in-app clears (api.ts 401 clears only the session token; verify-failure and the mount effect never clear a fresh openrouter record). Fix (frontend only, privacy-preserving, key still never leaves the device): `saveLLMConfig` now returns a persisted boolean (`try/catch` around `setItem` + same-tick read-back verify); new `canPersistLLMConfig()` probe; `SettingsPage` shows an actionable red banner + destructive toast on a blocked/ephemeral write (and proactively on mount) instead of falsely showing "Saved". Verified in a live browser: normal→persists, throwing→false, ephemeral→false; deployed prod chunk contains the banner/toast. No worker change. Rollback: `git revert e54a45e && git push origin production` (or redeploy prior tag `prod-2026-07-01-1420` = `ab1ea26`).

- `prod-2026-07-01-1749` — TWO fixes shipped together. (a) **Site Audit false "missing title/meta" behind a bot challenge** (worker-only), PR #74, merge `8412146`, worker version `7271ae25`. Bug `488cb637f2d3bc6004a62bd83c3d6c1d` (high, yalileecv@outlook.com, homesellingplus.com): the target site sits behind a JS proof-of-work anti-bot wall returning HTTP **218** + a ~2.8KB empty-`<head>` stub; DataForSEO crawled the stub so `analyzeOnPage` fired critical "Missing page title" + high "Missing meta description". Existing bot detection only fired on zero-pages / status >= 400, so a 2xx 218 slipped through as "completed". Fix: new `isLikelyBotChallenge(homepage)` (no title AND no description AND no h1 + a challenge signal: non-standard 2xx/3xx status like 218/202, or a content-less stub with no internal links + tiny size); `analyzeOnPage` suppresses the four stub-artifact findings (`missing_title`, `missing_meta_description`, `missing_h1`, `no_schema`) and emits one `bot_protection_partial_crawl` diagnostic; `buildStructuredSEO` sets `bot_protection_detected` from the same check (lights the existing `SEODashboard.tsx:1277` banner, was hardcoded false). 88/88 worker vitest. (b) **GSC empty-data + sync-error messaging** (SPA-only), PR #75, merge `81df14d`. Diagnosing angelanitkin@gmail.com (healthy connection, four brand-new `sc-domain:` sites with ~0 traffic): `SettingsPage.tsx` `handleSync` showed a red "No data found — try the sc-domain or www variant" (nonsensical for a Domain property that already covers all variants) and "Could not sync — Try reconnecting" on transient API errors. Now: neutral "No Search activity yet", Domain vs URL-prefix aware advice, a 0-rows "new site" message, and a "temporary API error, retry first" message for thrown syncs (an expired session redirects before that catch, so it is never an auth issue). Rollback worker: `wrangler rollback --message "bot-challenge regression" 9a952187`. Rollback SPA: `git revert 81df14d 8412146 && git push origin production` (or redeploy prior tag `prod-2026-07-01-1518` = `11873d9`).
- `prod-2026-07-01-1851` — Gap-analysis "AI Powered Insights" dead-Supabase fix (worker + SPA), PR #76, merge `21dd33d`, worker version `f1f9f80e`. Bug `bfe5d249` (medium, kevgraffiti@gmail.com, `/competitor-analysis?tab=gap-analysis`): "error getting ai insights". Root cause: `AIAnalysisSummary.tsx` still called the legacy Supabase edge function `keyword-analysis-ai`, whose Lovable AI gateway key (`LOVABLE_API_KEY`) was removed in the Cloudflare migration; the function still deploys but 500s "AI service not configured", so every "Generate AI Analysis" click errored (live-reproduced the 500). The Cloudflare replacement route existed but was orphaned (no caller) and returned a deterministic `ai_summary` in a different shape. Fix: rewrote the orphaned worker `/api/competitors/gap-analysis-ai` handler to run the original SEO-strategist prompt through server-side OpenRouter (`deepseek/deepseek-v4-flash`, platform-paid via `env.OPENROUTER_API_KEY`) and return `{ analysis: markdown }`; new pure `buildGapAnalysisPrompt()` (5 vitest cases, TDD); any LLM failure maps to a generic message so platform billing details never leak to users. SPA: repointed `AIAnalysisSummary` from `supabase.functions.invoke` to `api('/api/competitors/gap-analysis-ai')`, dropped the dead supabase import. 93/93 worker vitest; live prod chunk `CompetitorAnalysis-bIlT6bi5.js` verified to call the new endpoint with no `keyword-analysis-ai` reference. Follow-up (NOT done): 3 other components still call dead Supabase fns (`add-tracked-keywords`, `historical-rank-overview`, `get-user-emails`) and are likely broken the same way. Rollback worker: `wrangler rollback --message "gap-analysis-ai regression" 7271ae25`. Rollback SPA: `git revert 21dd33d && git push origin production` (or redeploy prior tag `prod-2026-07-01-1749`). NOTE: a follow-up sweep found the 3 "other dead Supabase callers" all live in UNREACHABLE code (old AddKeywordsDialog imported by nobody; HistoricalPerformance/get-user-emails/create-user only inside `ProjectTasks`/`SEOProjects`/`AdminUsers` pages that are not in the App.tsx router and not linked) — no additional live bug, optional cleanup only.
- `prod-2026-07-01-1918` — Content Writer interview loop fix (worker-only), PR #77, merge `453f8fc`, worker version `def27e4e`. Bug `ae77a909` (medium, ride@bradsbikes.com.au): interview "keeps circling back... asking the same question again and again". Proven from the live transcript (conv `70f731480b...`): from ~message 52 the model asked "any misconception about custom builds?" 20+ times while the user answered "no", and ignored the user pasting "Output the final document now" 3×. THREE root causes fixed in `handleInterview` (`workers/src/routes/content-writer.ts`) + `content-writer/prompts.ts`: (1) history query took the OLDEST 50 messages (`ORDER BY created_at ASC LIMIT 50`), so past ~50 messages the model stopped seeing the user's current answer + recent turns and re-ran from stale early context (loop went pathological at msg ~52, exactly at the window boundary) → changed to the most-RECENT 50 in chronological order (nested subquery + rowid tiebreak; validated on prod D1). (2) no rule to accept a negative/absent answer or stop → new universal `INTERVIEW_CONDUCT_RULES` appended to every interview system prompt (accept no/none/unknown as final + move on, never re-ask, one short follow-up max, stop immediately on a finish/finalize/"output the document" request). (3) blank replies (reasoning model spent its 2048-token budget on hidden reasoning) → budget 2048→4096, retry once on empty, friendly fallback (`resolveInterviewReply`) so the UI never renders an empty bubble. Model unchanged (deepseek-v4-pro) per product decision. 6 new vitest (99/99 total), tsc clean. Follow-up (NOT done): the finalize handler has the same oldest-N window (`LIMIT 100 ASC`) and could miss the tail of very long interviews. Rollback: `wrangler rollback --message "interview loop regression" f1f9f80e` (worker only; no SPA change to revert).
- `prod-2026-07-02-1401` — Wave 1+2 quality batch, FIVE PRs merged together (#80-#84), worker version `1b9ac56b`, frontend via CI run 28595657267. (a) **PR #80** app_events schema committed to VC (was live-D1-only; caused the silent 2026-05-14→06-13 telemetry blackout) + pruneAppEvents wired into the 6h cron. (b) **PR #81** SEO Assistant history window: newest-50 instead of oldest-50 (chat/handler.ts, same bug class as PR #77). (c) **PR #82** PR CI gate (pr-checks.yml): tsc + vitest + bundle guard check-only on every PR into production; enable branch protection to make it blocking. (d) **PR #83** dormant-account GSC purge: D1 was refilling toward the 10GB cap (~0.5GB/wk, cap ~mid-Aug); purges gsc_search_data for properties whose owner has no session in 60d (2,797 props), 10k-row chunks on the 6h cron, purged_at marker, auto-resync on return via GET /gsc/properties waitUntil + nightly cron. NEW D1 COLUMN `gsc_properties.purged_at` (migration `2026-07-02-gsc-purged-at.sql`, applied BEFORE this worker deploy, verified via pragma_table_info). (e) **PR #84** admin Activity dashboard (/admin/activity): 7 admin-gated /api/admin/activity/* endpoints over app_events (overview, per-tool adoption, users, activation funnel, logs, user detail, aggregate-only LLM summary with local fallback) + withCredit now forwards credit cost into app_events.credit_cost (starts accruing at this deploy). Verification: 106 worker vitest + 22 SPA vitest green on the merged tree; browser-verified against a seeded local stack; live post-deploy checks: /api/admin/activity/overview unauth = 401, POST /auth/google (Origin datawiseseo.com) = 200, datawiseseo.com = 200. Rollback worker: `wrangler rollback --message "wave1-2 regression" def27e4e`. Rollback SPA: redeploy prior tag `prod-2026-07-01-2206` (or git revert the five merge commits). The purged_at column is safe to leave in place after rollback (old code ignores it); the purge cron only starts deleting on the NEW worker, so rolling back the worker also stops the purge.
- `prod-2026-07-11-1924` — Blueprint Phase 2: persistence + durable orchestration (worker + SPA, all admin-gated and member-invisible), PR #98, merge `4a6312e`, worker version `848b7073`. Full blueprint-db schema (27 tables, schema_version=2, separate D1 `blueprint-db`), API idempotency store, budget reservations with atomic ceiling guard, stage leases with fencing epochs + CAS completion, 19-stage run state machine with stub handlers (zero provider calls), queue consumer with DLQ (`blueprint-research-dlq`) + delayed-send wake-ups for parked runs, projects/estimates/research-runs REST routes, admin preview UI at /blueprint with free stub research runs. Includes CORS fix `30ec193`: `Idempotency-Key` added to `Access-Control-Allow-Headers` (Phase 2 UI sends it on creates; preflight was blocking the POST as "Failed to fetch"). 326 worker vitest green. Rollback worker: `wrangler rollback --message "blueprint phase 2 regression" f8bd1169` (or `e3dd87db` for pre-Phase-2). Rollback SPA: `git revert 4a6312e && git push origin production` (or redeploy prior production head `3101107`). blueprint-db tables are isolated from datawise-db and safe to leave in place after rollback.

- `prod-2026-07-14-1901` — Blueprint Phase 3 real DataForSEO research + Meta Checker rewrite token-budget fix, TWO PRs merged together (worker + SPA, blueprint-db schema_version=3). (a) **PR #100** (merge `5abfa3e`): six real research stages (resolve_market, plan_research with fail-fast budget gate, collect_keyword_evidence, discover_competitors, collect_competitor_evidence, validate_serps_and_questions via async task_post/poll), every paid call through `blueprintDfsCall` (reserve-before-call, reconcile actuals, KV cache per catalog TTLs, R2 artifacts on paid calls only), estimates route planner-priced. Smoke-verified on staging 2026-07-12: full run $0.615 of $2.00 ceiling, 2,741 keywords (2,738 with volume), 19 competitors all with visibility_score, SERP snapshots with organic + related searches, reservations reconciled to 0; warm-cache rerun $0.0015. Known gap: competitor `estimated_traffic` (DFS etv) NULL on all rows, Phase 4 follow-up. (b) **PR #101** (merge `9b36c06`): meta-rewrite LLM output budget 1200→4000 tokens, doubling to 8000 on `finish_reason=length` instead of same-size re-ask (DeepSeek V4 Pro hidden reasoning was eating the whole budget, bug "output was cut off"). Worker version `b901b103-306d-4046-b561-f76c0dfd5062`. **Gotcha proven live 2026-07-14: the worker is shared, so deploying it from any branch without Phase 3 silently reverts Blueprint to stub handlers** (the 07-13 meta-rewrite hotfix deploy `3c2b5dfb` did exactly that; a stub run writes `{"stub":true}` stage output, 0 keywords, $0 spend). Always deploy the worker from `production` HEAD once PRs are merged. Rollback worker: `wrangler rollback --message "phase 3 / meta-rewrite regression" 3c2b5dfb` (keeps meta-rewrite fix, Blueprint back to stubs) or `cf401779` (pre-both). Rollback SPA: `git revert 9b36c06 5abfa3e && git push origin production` (or redeploy prior tag `prod-2026-07-11-1924`).

## Rollback (Worker, `datawise-api`)

The Worker is independent of Pages. To roll back:

```sh
# 1. List versions (most recent first)
cd datawise-seo-insight-main/workers
CLOUDFLARE_ACCOUNT_ID=510d0ac03a3a8f5ebeac39be4926ed77 \
  npx wrangler deployments list | head -30

# 2. Roll back to a prior version id
CLOUDFLARE_ACCOUNT_ID=510d0ac03a3a8f5ebeac39be4926ed77 \
  npx wrangler rollback --message "rollback reason" <version-id>
```

Last known-good worker versions:

- `e9ef6067` — 2026-05-17 baseline before the May 2026 changes.
- `316dc206` — added `DataForSeoQuotaError` typed 503 (2026-05-21).
- `012530e8` — caching + cron + quota short-circuit (2026-05-21).
- `fa2c7495` — OpenRouter preflight returns 400 (not 401) on invalid key, stops SPA forced logout (2026-05-27, PR #25).
- `64ae2d0e` — GSC cluster 1 — atomic disconnect, refuse empty refresh_token on OAuth callback, refresh_failed_at tracking, needs_reconnect/has_orphan_properties in /gsc/properties response, gsc_reauth_required code on 403s (2026-05-27, PR #26).
- `fd479d13` — Meta Checker apostrophe truncation fix — backreference regex for HTML attribute quote matching (2026-05-27, PR #28).
- `e3dd87db` — Blueprint Phase 0+1 bindings + admin gate + health route (2026-07-10, PRs #96/#97).
- `848b7073` — Blueprint Phase 2 orchestration + Idempotency-Key CORS fix (2026-07-11, PR #98).
- `b901b103-306d-4046-b561-f76c0dfd5062` — current; Blueprint Phase 3 real research + meta-rewrite token budget (2026-07-14, PRs #100/#101). Do NOT roll back past this while Blueprint is in use: older versions run stub handlers.

If a rollback reintroduces an old bug, also `git revert` the corresponding commit on `production` so the source matches the live worker. Otherwise the next deploy via CI re-ships the bad change.

## Roll back today's specific changes individually

If the caching layer specifically misbehaves (stale data complaints, KV cost spike), the smallest reversal is:

```sh
git revert 4a51176   # perf(workers): cache + cron + quota short-circuit
git push origin production
# CI rebuilds SPA (no-op), then run the worker deploy manually:
cd datawise-seo-insight-main/workers && npm run deploy
```

If the typed quota error layer misbehaves:

```sh
git revert 2d6cebb   # fix(rank-tracking): provider_quota_exhausted 503
git push origin production
cd datawise-seo-insight-main/workers && npm run deploy
```

If the marketing CL0 handler misbehaves:

```sh
git revert fbd10c2   # fix(marketing): ship Resend CL0 handler
git push origin production
# Then rebuild + redeploy datawise-marketing manually:
cd marketing && npm run build && npx wrangler pages deploy ./dist \
  --project-name=datawise-marketing --branch=main
```

## What NOT to do

- ❌ `wrangler pages deploy dist --project-name=datawise ...` from any branch. Use the npm script.
- ❌ Deploy from a feature branch.
- ❌ Deploy with a dirty working tree (`git status` must be clean).
- ❌ Deploy from `main` (it's stale).
- ❌ Deploy production before verifying a preview/staging URL.
- ❌ Use the live app as the first place to test a change.
- ❌ Skip the PR step "just for a one-liner". The PR diff is the only place you'll catch deleted files.
- ❌ Use `git add .` or `git add -A`. Only add the files you intended to change.

## What an AI agent (Claude/Codex) must do before deploying

1. Run `git rev-parse --abbrev-ref HEAD` → must be `production`.
2. Run `git status --porcelain` → must be empty.
3. Run `git fetch origin && git rev-list HEAD..origin/production --count` → must be 0.
4. Confirm with the user in plain English: "I'm about to deploy commit `<short-sha>` (`<commit-message>`) to production via `npm run deploy:pages:production`. Proceed?"
5. Only after explicit user "yes" — run `npm run deploy:pages:production`.
6. After success, tag the deploy as above.

If any of steps 1-3 fail, stop and tell the user. Do not attempt to fix the state automatically.
- `prod-2026-07-03-0002` — AI Visibility Performance tab redesign (SPA-only, no worker change, no migration), PR #85, merge `b845bf4`, frontend via CI on push to production. New default Performance tab on /ai-visibility implementing the approved Claude Design handoff (v2 dense layout + v1 extras): KPI rail (Right Now fraction, Score with delta + formula tooltip, Cited, Best Engine), Answer Status Matrix (query x engine cells with citation position, per-row 0-100 score, expandable proof: excerpt, per-answer citation chips with own-domain highlight, lazy full answer, recommendation, stop tracking), right rail (baseline-anchored composition Trend with 7/30/90 + single-check fallback, Share of Voice with You pill + not-on-board state), brand-terms/engines strip with engine logos (Google G, OpenAI knot, Perplexity mark; simple-icons CC0), discovery card (Terms You're Cited For, honest historical-index copy). Behavior changes: auto first check on load for projects with queries but zero history (server 24h freshness guard prevents double spend); Check now labeled with 1-credit cost; multiple projects on one domain now ranked (tracking enabled > brand terms set > oldest) with a visible switcher instead of silently binding to the newest duplicate (this had bound Nicolas's tab to an unconfigured duplicate and under-detected mentions); Track buttons on Brand Tracker answer rows + Instant Check results feed the tracker. Replaced VerdictStrip/EngineTrendChart/QueryCard/ShareOfVoiceFooter/RecommendationBlock; deploy guard updated to pin AnswerStatusMatrix + KpiRail (old EngineTrendChart guard string removed). Verification: 22 SPA vitest + tsc + bundle guard green, staged and browser-verified on staging.datawise-118.pages.dev across rich (5 queries x 4 weeks) and cold-start (2 queries x 1 check) datasets, design-reviewed by Nicolas on staging through 4 feedback rounds; live prod chunk verified to contain the new matrix strings. Rollback SPA: `git revert b845bf4 && git push origin production` (or redeploy prior tag `prod-2026-07-02-1401`). No worker rollback needed.
- `prod-2026-07-03-1427` — AI Visibility clarity pass (SPA-only, no worker change, no migration), PR #87, merge `a7b58d3`, frontend via CI run 28666618352. Brand Tracker now follows the top-left site selector (was: silently restoring the last-typed domain from localStorage `brand-tracker:active-domain`, so it could show a different website than the rest of the app); syncs on load and on every selection change; analyzing another domain still works and shows an amber banner ("Viewing X, not your selected website") with a one-click Back button; no-property behavior unchanged. Both tabs gained plain-English explainers (Performance = your tracked queries checked weekly across Google AI Mode/ChatGPT/Perplexity; Brand Tracker = the historical AI search index, no setup), a note that the two data sources will not match, and a discover-then-track hint above the answers table. Verification: tsc + 22 SPA vitest + build green, staged and verified on staging.datawise-118.pages.dev. Rollback SPA: `git revert a7b58d3 && git push origin production` (or redeploy prior tag `prod-2026-07-03-0002`). No worker rollback needed.
- `prod-2026-07-03-1516` — Content Writer GEO hardening wave 1 (SPA + worker + D1 migration), PR #86, merge `82de040`, worker version `27b425b9` (prior deploys this wave: `fba28dc5`), frontend via CI run 28669134892. D1: 3 additive columns on content_writer_posts (`review_json`, `seo_title`, `seo_meta_description`, migration `2026-07-03-content-writer-geo-wave1.sql`, applied + pragma-verified BEFORE worker deploy). Features: Review QA step exposed as pipeline button 4 + report persisted to review_json (cleared by upstream re-runs); opening answer capsule (40-60 words + number, above TL;DR) required in every draft; `{{current_date}}` placeholder ends hallucinated dates in finalized KB docs (bug `56bd3cb3`); handleFinalize now reads the most RECENT 100 interview messages (same class as PR #77); primary-source research rules + Find-different-sources with deterministic rejected-domain citation filter (feedback `16d1a7c3`); client-side source tier badges (Primary/Official docs) + zero-primary warning; SEO title/meta endpoint POST /posts/:id/meta (1 credit) AND auto-generated with every successful draft at no extra credit (request `4d41529f`); outline max_tokens 4096→8192 (field cutoff); outline tags ([CAPSULE]/[NARRATIVE]/[TABLE]) deterministically stripped from draft/review output; DraftProgressBar realistic outline ETA + pulsing still-working state. Verification: 135 worker vitest + 25 SPA vitest + tsc both + build green; whole-branch code review with 2 Important findings fixed pre-merge; prod D1 confirmed to have NO published prompt overrides shadowing the changed prompt keys; staged + user-verified on staging.datawise-118.pages.dev (found the outline cutoff + tag leak, fixed in `59f432a`). Rollback SPA: `git revert 82de040 && git push origin production` (or redeploy prior tag `prod-2026-07-03-1427`). Rollback worker: `wrangler rollback --message "cw wave1 regression" <pre-wave version>` (version before `fba28dc5`). The 3 D1 columns are safe to leave after rollback (old code ignores them).
- `prod-2026-07-08-1222` — Bug batch 5 (SPA + worker, no migration), PR #88, merge `9565002`, worker version `daec0c16`, frontend via CI run 28942006957. Seven feedback bugs: (1) `b1c1b4d9` Keyword Research filters (keyword/location/language) shared across all tabs via new `useKeywordFilters` hook (`keyword-research:*` localStorage keys, seeded from Settings defaults). (2) `ee89b0bb` removed `forceMount` from the 7 KeywordResearch tab panels; 5 simultaneously-mounted forms shared `id="keyword"`, breaking label/focus resolution and scrolling the page while typing; Related/PAA/Fan-out results converted to usePersistentState so tab-switch unmount keeps runs. IMPORTANT: the deploy guard's 7 forceMount markers were REPLACED with persistent-state markers; do not re-add forceMount. (3+4) `5a49fe13`/`64fe1477` SEO Assistant prompt chips auto-send via `handleSend(textOverride)`, stay visible mid-conversation, disabled while streaming. (5) `56bd3cb3` Content Writer dates: finalize sends the client local date (validated YYYY-MM-DD, precedence over server UTC in `buildWriterPromptContext`), card timestamps parse D1 UTC strings via `parseUtc`. (6) `f8ce71ad` `/tasks` route registered (placeholder page existed, route missing → NotFound). (7) `cc2c07fc` keyword table exports format cpc/competition via shared metric formatters; `1a856da2` table sections gained optional `colWidthsPct`, Site Audit findings tables use 14/36/50 so autotable stops starving Severity/Issue. Verification: 137 worker vitest (2 new client-date cases), tsc clean on touched files, deploy:pages:check green, staging browser-verified (filter sharing across 3 tabs, no scroll jump, /tasks renders, two chip sends in one chat). Rollback SPA: `git revert 9565002 -m 1 && git push origin production` (or redeploy prior tag `prod-2026-07-03-1516`). Rollback worker: `wrangler rollback --message "bug batch 5 regression" 27b425b9` (client_date is optional; old worker ignores it, so SPA and worker roll back independently).
- `prod-2026-07-08-1300` — Traffic Trends tab (SPA + worker, no migration), PR #90, merge `d96cf13`, worker version `e3b45bac`. New Competitor Analysis tab showing historical estimated monthly organic traffic (Ahrefs-style) for up to 10 domains over 6/12/24 months, worldwide by default or market-scoped. Worker: `POST /api/competitors/traffic-history` → DFS Labs `historical_bulk_traffic_estimation`, KV cache 24h, 1 credit, `buildTrafficHistorySeries` transform unit-tested (139/139 vitest). SPA: `src/pages/TrafficHistory.tsx` (summary cards, multi-line recharts with distinct series colors, CSV export, persistent state, panel not forceMounted). Closes feedback `a369f5f2` (imregabri). Staging-verified with calixpert.com (29,359 est. visits, +82%, 12-month trend line) and a 2-domain comparison. Rollback SPA: `git revert d96cf13 -m 1 && git push origin production` (or redeploy prior tag `prod-2026-07-08-1222`). Rollback worker: `wrangler rollback --message "traffic trends regression" daec0c16` (route is additive; SPA and worker roll back independently).
- `prod-2026-07-08-1841` — Backlinks per-URL filtering + distinct competitor colors (SPA-only, no worker change, no migration), PR #92, merge `7ef9977`, frontend via CI run 28966777437. (1) `0356cc2a` (imregabri, critical): the Backlinks input now accepts an exact page URL. `cleanBacklinksTarget` preserves the path and sends a DFS webpage target (absolute URL, host/path casing preserved; bare domains unchanged), so every tab (overview, list, referring domains, anchors, gap) scopes to that page; a "Page mode" badge under the input clarifies scope. New sortable "Links to" column in the backlinks list renders `url_to` as the path with a full-URL tooltip + external link, showing which page on the analyzed site each backlink points at. Anchor brand detection now derives the brand root from the host so it works for URL targets. (2) `25747527` (Ron): Competitive Profile radar, Position Distribution bars, winner cards, and the Full Metrics table in Domain Rank now use the far-apart Traffic Trends palette instead of five near-identical pastels. Verification: tsc clean on touched files, deploy:pages:check green, staging browser-verified (page mode with ahrefs.com/blog/seo-statistics/ returned 2.9K page-scoped backlinks; 5-domain comparison shows clearly distinct colors), live prod spot-checked after CI deploy. Rollback SPA: `git revert 7ef9977 -m 1 && git push origin production` (or redeploy prior tag `prod-2026-07-08-1300`). No worker rollback needed.
- `prod-2026-07-09-1528` — Rank Tracking UX wave (SPA + worker, no migration), PR #94, merge `c3428c6`, worker version `cf401779`. Three feedback items: (1) `abf9b69f` (john@captivatewebsites, partial) sidebar website dropdown now sorts alphabetically by cleaned domain (`AppSidebar.tsx`); the search box half of the request is a follow-up (needs Select→Combobox swap preserving the per-item delete affordance). (2) `d4290fa7` (imregabri) Add Keywords dialog gained Import CSV: first column parsed client-side (quoted fields + `""` unescape), header row skipped, case-insensitive dedupe against typed keywords, merged into the one-per-line textarea for review; backend untouched (already accepts arrays — paste-a-list worked all along). (3) `650773fd` (imregabri, "almost useless" without pairing) Page 2 Opportunities rows are now expandable: `/gsc/queries?filter=page2&page=<url>` (new additive branch in `handleGSCQueries`, mode `page_queries`, top 50 queries by impressions, same `baseWhere` as the parent aggregate so numbers reconcile) renders the page's striking-distance queries inline with a per-query Track button (`GSCQueryTable.tsx` expandable rows). Plus `ProjectStatsCards` DeltaBadge rounds to ≤2 decimals (was rendering raw float `6.300000000000001`). Verification: tsc clean frontend + worker, deploy:pages:check green, staging browser-verified (alpha order, CSV import via synthetic file event incl. header-skip + dedupe, drill-down expanded /post/perplexity-seo/ with live queries, delta badge shows 6.3). Rollback SPA: `git revert c3428c6 -m 1 && git push origin production` (or redeploy prior tag `prod-2026-07-08-1841`). Rollback worker: `wrangler rollback --message "rank tracking ux regression" e3b45bac` (drill-down param is additive; SPA and worker roll back independently).
- `prod-2026-07-16-1428` — Blueprint Phase 4: clustering + page-planning engine (worker + D1 migration, no SPA change), PR #103, merge `124c3de`, worker version `9a327a68` (deployed from branch tip `72217a8`, byte-identical to production HEAD: merge-base check confirmed zero worker changes on production since branch cut). D1: migration `2026-07-14-phase4.sql` applied + verified BEFORE worker deploy (keywords.excluded_reason; keyword_clusters.ruleset_version + score_breakdown_json; new tables cluster_adjudications, parsed_competitor_pages, existing_pages; schema_version 4). All 8 Phase 4 stages now real: normalize_keyword_universe, embed_keyword_features (Workers AI @cf/baai/bge-m3 via new [ai] binding, free), build_provisional_clusters (REQUIRED), per-cluster SERP representative queries, refine_clusters, parse_competitor_pages, build_page_plan (REQUIRED), overlay_existing_site, validate_blueprint (REQUIRED), publish_blueprint materializes blueprint_pages. Versioned rulesets cluster-v2 + pp-v1 (pinned-hash drift tests; composite stamp cluster-v2+pp-v1, schema p4). cluster-v2 was calibrated on real run vectors after cluster-v1's first live run produced a 614-keyword mega-cluster: semantic-only merge floor 0.85, adjudication floor 0.75, recut ceiling to 1.00, adjudication persistence cap 2000 (truncation surfaced as warning). Ops rides: competitor estimated_traffic extraction, per-stage cost_usd_micro attribution, OPERATION_STAGE centralization. Verification: 959 worker vitest / 95 files, tsc clean, boundary script clean, per-task adversarial reviews (21 tasks) + whole-branch final review + cluster-v2 review, two live smoke runs (run_3c63faa9 caught the mega-cluster; run_7325f00e verified the fix: max cluster 29 vs 614, service-location pages keyword-aligned, adjudications capped, 30 pages unique slugs/PKs, publish stamp cluster-v2+pp-v1). Phase 5 stubs remain: collect_us_fanout, synthesize_page_briefs (runs finish partial by design). GOTCHA still applies: deploying the worker from a branch without Blueprint code reverts Blueprint to stubs. Rollback worker: `wrangler rollback --message "phase4 regression" <pre-phase4 version>` (D1 additions are additive; safe to leave). No SPA rollback needed.
- `prod-2026-07-17-1255` — Content Writer SEO title/meta generation fix (worker-only, no SPA change, no migration), PR #105, merge `22a2201`, worker version `fbecd974-daa2-4eb7-b988-3ac2cdd9c6d1`. Bug `fff8305f` (amir@aimevolve.com): both SEO meta call sites (auto after draft + `POST /posts/:id/meta`) used `max_tokens=512` against deepseek-v4-pro, whose hidden reasoning tokens spend the same budget, so the visible JSON came back empty/truncated, `parseSeoMetaResponse` returned null, and every retry 502'd with "could not parse title and meta". Fix: `resolveSeoMetaMaxTokens` (2048 default, 8000 for gpt-5.5-pro's mandatory reasoning), `response_format: json_object`, and a distinct error + log line on `finish_reason=length`. Same failure family as the 07-14 meta-rewrite fix (PR #101). Verification: 962/962 vitest, tsc clean, live browser regenerate on prod returned "Title and meta generated". Rollback worker: `wrangler rollback --message "seo meta regression" 9a327a68` (no SPA rollback needed).
- `prod-2026-07-17-1310` — Escalating LLM token budgets for remaining starved call sites (worker-only, no SPA change, no migration), PR #107, merge `17deaa3`, worker version `aaecdf87-b506-4694-800c-1ffe45583fa9`. Follow-up to PR #105: same reasoning-token starvation class at three more sites. Content Revival analyze (was 1024, JSON-parsed with a silent junk-audit fallback on truncation), section regeneration (was 2048), gap-analysis AI (was 2000). New shared `chatCompleteEscalating` in `llm/length-escalation.ts`: 4000 start, doubles to 8000 on `finish_reason=length` (max 3 attempts), usage summed, truncations logged with route label + model; analyze also gets `response_format: json_object`. Verification: 967/967 vitest (5 new helper tests), tsc clean, live prod browser run of Content Revival end-to-end (analyze completed at ~3.4K total tokens, rewrite rendered). Rollback worker: `wrangler rollback --message "llm escalation regression" fbecd974` (no SPA rollback needed).
