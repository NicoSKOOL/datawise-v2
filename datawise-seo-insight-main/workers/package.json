{
  "name": "datawise-workers",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "dev:mcp": "wrangler dev -c wrangler.mcp.toml --port 8788 --host localhost:8788",
    "deploy": "node scripts/guard-worker-deploy.mjs && wrangler deploy",
    "deploy:preview": "wrangler versions upload --preview-alias staging",
    "deploy:mcp": "node scripts/guard-worker-deploy.mjs && wrangler deploy -c wrangler.mcp.toml",
    "deploy:staging": "echo 'Removed: this created an orphan worker. Use npm run deploy:preview (test) or merge to production (live).' && exit 1",
    "deploy:production": "echo 'Removed: this created an orphan worker. Use npm run deploy:preview (test) or merge to production (live).' && exit 1",
    "db:migrate": "wrangler d1 execute datawise-db --file=src/db/schema.sql",
    "db:migrate:staging": "wrangler d1 execute datawise-db-staging --file=src/db/schema.sql",
    "db:migrate:production": "wrangler d1 execute datawise-db-prod --file=src/db/schema.sql",
    "test": "vitest run",
    "llm-models:snapshot": "node scripts/refresh-llm-models-snapshot.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260702.1",
    "@types/better-sqlite3": "^7.6.13",
    "better-sqlite3": "^12.11.1",
    "typescript": "^5.8.3",
    "vitest": "^3.2.6",
    "wrangler": "~4.107.1"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "0.10.3",
    "@modelcontextprotocol/sdk": "1.30.0",
    "@modelcontextprotocol/server": "2.0.0",
    "agents": "^0.22.0",
    "zod": "^4.4.0"
  }
}
